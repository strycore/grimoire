{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/strycore/grimoire/schema/spell.schema.json",
  "title": "Grimoire Spell",
  "description": "A grimoire spell — one declared state. Source of truth for the on-disk YAML format. Mirrors `Spell` in src/spell.rs. CI uses this to validate every shipped spell, and IDEs (yaml-language-server) use it for autocomplete and inline lint while authoring.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "summary", "verify", "cast"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Unique spell identifier. Kebab-case, must start with a lowercase letter."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "One-line description shown in listings."
    },
    "verify": {
      "type": "string",
      "minLength": 1,
      "description": "Bash snippet that exits 0 iff the desired state is currently reached."
    },
    "version_check": {
      "type": "string",
      "description": "Bash snippet that prints the currently-installed version on stdout. Required for any manifest entry that uses a version constraint."
    },
    "description": {
      "type": "string",
      "description": "Long-form documentation, shown in `grimoire show` and the GUI detail view."
    },
    "homepage": {
      "type": "string",
      "format": "uri"
    },
    "maintainer": {
      "type": "string",
      "description": "Contact for upstream issues with the spell itself."
    },
    "category": {
      "type": "string",
      "description": "Free-form grouping label for the GUI and `grimoire ls --category`."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Free-form tags for search and filtering."
    },
    "distros": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Distros this spell is known to work on. Empty = universal. Tool checks /etc/os-release."
    },
    "requires": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Constraint on another spell. Same syntax as manifest entries: 'name', 'name >= 1.0', 'name >= 1.0, < 2.0'."
      }
    },
    "system_requires": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Bare binary name (e.g. 'cc', 'pkg-config', 'make', 'curl', 'wget', 'git') that must be in PATH before this spell can cast. Missing binaries are installed via the distro package manager (dnf/apt/pacman). Not modeled as full spells."
      }
    },
    "before": {
      "type": "string",
      "description": "Bash snippet run before any cast channel."
    },
    "after": {
      "type": "string",
      "description": "Bash snippet run after the chosen cast channel succeeds. Failures in `after` are warned but don't fail the cast."
    },
    "cast": {
      "type": "object",
      "additionalProperties": false,
      "required": ["default", "channels"],
      "properties": {
        "default": {
          "type": "string",
          "description": "Channel key used when none is specified by the user or manifest. Must match a key in `channels`."
        },
        "channels": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/channel" },
          "description": "One or more named install channels. Keys are referenced by `--via <key>` and by manifest overrides."
        }
      }
    },
    "provides": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "binaries": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "description": "Informational. Used for cross-spell reasoning later (e.g. transitive `provides` could obviate `requires`)."
    },
    "desktop": {
      "$ref": "#/$defs/desktop_entry"
    }
  },
  "$defs": {
    "channel": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "summary"],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "shell",
            "dnf",
            "apt",
            "pacman",
            "flatpak",
            "snap",
            "cargo",
            "pip",
            "npm",
            "compose"
          ],
          "description": "Channel kind. Drives GUI badges, channel-specific update probes, and implicit distro applicability (e.g. `dnf` ⇒ Fedora-family)."
        },
        "summary": {
          "type": "string",
          "description": "Per-channel description shown when listing alternatives."
        },
        "run": {
          "type": "string",
          "minLength": 1,
          "description": "Bash snippet executed to install via this channel. Required for shell-style channels; forbidden for `compose`."
        },
        "version_hint": {
          "type": "string",
          "enum": ["latest", "distro", "pinned"],
          "description": "What kind of version this channel typically delivers. Helps the tool pick a channel when a constraint isn't satisfied."
        },
        "requires_sudo": {
          "type": "boolean",
          "description": "Set true if the channel's `run` invokes sudo. Informational; the tool does not invoke sudo on the channel's behalf."
        },
        "distros": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Distros this channel applies to. Empty/omitted ⇒ use the implicit list from `type` (e.g. `dnf` ⇒ Fedora-family). Set this only to override that default — for instance to restrict a `shell` channel to one distro, or to broaden a `dnf` channel to OpenSUSE forks."
        },
        "repo": {
          "type": "string",
          "format": "uri",
          "description": "Compose channel only. Git URL to clone for the compose source."
        },
        "ref": {
          "type": "string",
          "minLength": 1,
          "description": "Compose channel only. Git ref (tag, branch, commit SHA) to pin to."
        },
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Compose channel only. Subdirectory inside `repo` containing the compose file. Use `.` for repo-root composes."
        },
        "compose_file": {
          "type": "string",
          "minLength": 1,
          "description": "Compose channel only. Compose filename within `path`. Defaults to `docker-compose.yml`."
        },
        "parameters": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/parameter" },
          "propertyNames": {
            "pattern": "^[A-Z_][A-Z0-9_]*$",
            "description": "Env var name (uppercase, underscores, digits)."
          },
          "description": "Compose channel only. Declared environment parameters. Materialized into a `.env` next to the compose file."
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "type": { "const": "compose" } },
            "required": ["type"]
          },
          "then": { "required": ["repo", "ref", "path"] },
          "else": { "required": ["run"] }
        }
      ]
    },
    "parameter": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "summary": {
          "type": "string",
          "description": "One-line description shown in prompts and `grimoire show`."
        },
        "default": {
          "type": "string",
          "description": "Used when the user hasn't otherwise set the parameter. Mutually exclusive with `kind: secret` and `required: true`."
        },
        "kind": {
          "type": "string",
          "enum": ["secret"],
          "description": "`secret` ⇒ generated on first cast and persisted in the service `.env`."
        },
        "required": {
          "type": "boolean",
          "description": "Must be supplied (no default, not a secret). Cast fails fast if no value is resolved."
        }
      }
    },
    "desktop_entry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "exec"],
      "description": "XDG menu entry materialized after a successful cast. Maps to a `.desktop` file at `~/.local/share/applications/<spell>.desktop`.",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "`Name=` — human-readable label shown in app menus."
        },
        "exec": {
          "type": "string",
          "minLength": 1,
          "description": "`Exec=` — command line, including any `%F`/`%U` field codes."
        },
        "comment": {
          "type": "string",
          "description": "`Comment=` — tooltip text. Falls back to the spell's `summary` when omitted."
        },
        "icon": {
          "type": "string",
          "description": "`Icon=` — stem name (looked up in grimoire's bundled `icons/` and in `~/.config/grimoire/icons/`) or an absolute path to a file the spell author placed."
        },
        "categories": {
          "type": "array",
          "items": { "type": "string" },
          "description": "`Categories=` — XDG menu categories (e.g. `Development`, `Science`)."
        },
        "terminal": {
          "type": "boolean",
          "description": "`Terminal=` — true for CLI apps that need an emulator window."
        },
        "startup_wm_class": {
          "type": "string",
          "description": "`StartupWMClass=` — used by some compositors to associate the launched window with this entry (icon-grouping in the dock)."
        },
        "mime_types": {
          "type": "array",
          "items": { "type": "string" },
          "description": "`MimeType=` — file types this entry can open."
        }
      }
    }
  }
}
